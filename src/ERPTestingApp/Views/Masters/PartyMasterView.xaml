<UserControl x:Class="ERPTestingApp.Views.Masters.PartyMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form Section -->
        <Border Grid.Row="0" BorderBrush="#BDC3C7" BorderThickness="1" Padding="15" Background="White" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0">
                    <Label Content="Party Name *" FontWeight="Bold"/>
                    <TextBox Text="{Binding PartyName, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1">
                    <Label Content="Contact Person"/>
                    <TextBox Text="{Binding ContactPerson, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0">
                    <Label Content="Email"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1">
                    <Label Content="Address"/>
                    <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="➕ Add" Command="{Binding AddCommand}" 
                           Background="#28A745" Foreground="White" Width="100"/>
                    <Button Content="💾 Update" Command="{Binding UpdateCommand}" 
                           Background="#007BFF" Foreground="White" Width="100"/>
                    <Button Content="❌ Cancel" Command="{Binding CancelCommand}" 
                           Background="#6C757D" Foreground="White" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- List Section -->
        <Border Grid.Row="1" BorderBrush="#BDC3C7" BorderThickness="1" Background="White">
            <DataGrid ItemsSource="{Binding Parties}" 
                     SelectedItem="{Binding SelectedParty}"
                     AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Party Name" Binding="{Binding PartyName}" Width="180"/>
                    <DataGridTextColumn Header="Contact" Binding="{Binding ContactPerson}" Width="150"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✏️ Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                           Background="#FFC107" Foreground="Black" Margin="2" Padding="5,2"/>
                                    <Button Content="🗑️ Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                           Background="#DC3545" Foreground="White" Margin="2" Padding="5,2"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
